<%= form_with(model: stores_book) do |form| %>
  <% if stores_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stores_book.errors.count, "error") %> prohibited this stores_book from being saved:</h2>

      <ul>
        <% stores_book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
